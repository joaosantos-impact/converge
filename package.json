{
  "name": "converge",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -k -n web,api -c cyan,green \"cd frontend && bun run dev\" \"cd backend && bun run start:dev\"",
    "build": "cd frontend && bun run build && cd ../backend && bun run build",
    "build:web": "cd frontend && bun run build",
    "build:api": "cd backend && bun run build",
    "start": "concurrently -k -n web,api -c cyan,green \"cd frontend && bun run start\" \"cd backend && bun run start:prod\"",
    "lint": "cd frontend && bun run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}

{
  "name": "converge",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -k -n web,api -c cyan,green \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "build:web": "npm run build --prefix frontend",
    "build:api": "npm run build --prefix backend",
    "start": "concurrently -k -n web,api -c cyan,green \"npm run start --prefix frontend\" \"npm run start:prod --prefix backend\"",
    "lint": "npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
